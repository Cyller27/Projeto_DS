create database cleanall;
use cleanall;

create table usuario (
	id_usuario int not null auto_increment primary key,
	nome varchar(15) not null,
    senha varchar(25)
);

create table produto(
	id_produto int not null auto_increment primary key,
    nome varchar(30) not null,
    preco decimal(10,2) not null,
    categoria varchar(30)
);

insert into usuario (nome, senha)
values 
	('admin', 'admin'),
    ('Felipe', '1234'),
    ('Gabriel', '2021'),
    ('Brendon', '2020'),
    ('Ivan','amoDS');
    
insert into produto (nome, preco, categoria)
values
	('Detergente líquido','4','Limpeza Doméstica'),
	('Sabão em pó','3.5','Limpeza Doméstica'),
	('Sabonete','2.8','Higiene Pessoal');

select * from usuario;
select * from produto;
update usuario set senha = 'amoDS' where id_usuario = 4;
drop table usuario;
drop table produto;

delete from usuario;
truncate table usuario;

drop database cleanall;

